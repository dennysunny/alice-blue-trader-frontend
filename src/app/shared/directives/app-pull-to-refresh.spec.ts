import { AppPullToRefresh } from './app-pull-to-refresh';

describe('AppPullToRefresh', () => {
  it('should create an instance', () => {
    const directive = new AppPullToRefresh();
    expect(directive).toBeTruthy();
  });
});
